trigger: none

pool:
 vmImage: 'ubuntu-latest'

stages:
 - stage: 'Build'
   jobs:
    - job: 'installandbuild'
      steps:
           
      - task: Npm@1
        inputs:
         command: 'install'
        
      - task: Npm@1
        inputs:
          command: 'custom'
          customCommand: 'run build'